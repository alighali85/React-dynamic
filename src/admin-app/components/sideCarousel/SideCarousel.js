import React from 'react'
import PropTypes from 'prop-types'

const propTypes = {}

const defaultProps = {}

export default class SideCarousel extends React.Component {
  constructor (props) {
    super(props)
    this.state = {}
  }

  render () {
    return (
      <React.Fragment />
    )
  }
}

SideCarousel.propTypes = propTypes
SideCarousel.defaultProps = defaultProps
